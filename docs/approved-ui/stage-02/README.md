# Frozen Stage 2 approved references

These are byte-for-byte copies of the approved v1 AUTH-01 through AUTH-09 desktop, mobile, and states HTML (27), original CSS and required licensed assets. `manifest.json` records original SHA-256 hashes; the approved master PDF remains visual authority. Reference review bars/device frames/annotations are presentation, not product UI.

Do not update these files to make failing product screenshots pass. Any change requires explicit Rex-approved versioned supersession. The later approved five-language selector, inline password-eye controls, email branding, and documented v2 MFA subviews are amendments, not additional canonical screens. Test the application against these references and the recorded amendments.

Run `python3 scripts/check_ui_reference_manifest.py` to verify source integrity. This integrity check is NOT a visual acceptance test: Playwright screenshots, state/action checks, desktop/mobile and RTL review, and Rex acceptance remain mandatory. See `docs/ui-acceptance.md`.
